// constants are created here and later have to be imported in actions.js and reducers.js.

// only one constant needed for synchronous action.
export const CHANGE_SEARCH_FIELD = 'CHANGE_SEARCH_FIELD';

// three constants needed for assynchronous action (pending, success, fail).
export const REQUEST_ROBOTS_PENDING = 'REQUEST_ROBOTS_PENDING';
export const REQUEST_ROBOTS_SUCCESS = 'REQUEST_ROBOTS_SUCCESS';
export const REQUEST_ROBOTS_FAIL = 'REQUEST_ROBOTS_FAIL';

